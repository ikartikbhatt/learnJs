//DOM -- document object model

//selection of elements


//select element using get element by id

//it will only work for the id that is unique in the html document

//const mainHeading= document.getElementById('mainHeading');

//select element using querry selector
//it will work for all the elements that match the selector

//const mainHeading = document.querySelector("#mainheading");
//console.log(mainHeading); //it will return the element having mainheading class



//diff btw querySelector & querySelectorAll

//querySelector will return the first element that matches the selector

const navitem = document.querySelector(".nav-item");
//console.log("querrySlector", navitem); //return the first class of out of all the class with nav-item class


//querySelectorAll will return all the elements that match the selector

const navitems = document.querySelectorAll(".nav-item");
//console.log("querrySlectorAll", navitems); //return all the class of out

























